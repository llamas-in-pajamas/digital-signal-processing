<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="Digital Signal Proessing" Height="640" Width="1024">
    <Border Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="*" />
                <ColumnDefinition  Width="3*" />
            </Grid.ColumnDefinitions>
            
            <!-- User controls -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Signal" />
                <ComboBox>
                    <ComboBoxItem>Sinus</ComboBoxItem>
                    <ComboBoxItem>Sinus1P</ComboBoxItem>
                    <ComboBoxItem>Sinus2P</ComboBoxItem>
                    <ComboBoxItem>Rectangular</ComboBoxItem>
                    <ComboBoxItem>Symetric Rectangular</ComboBoxItem>
                    <ComboBoxItem>Unit Jump</ComboBoxItem>
                    <ComboBoxItem>Triangular</ComboBoxItem>
                    <ComboBoxItem>Stady Noise</ComboBoxItem>
                    <ComboBoxItem>Gaussian Noise</ComboBoxItem>
                    <ComboBoxItem>Impulse Noise</ComboBoxItem>
                    <ComboBoxItem>Unit Impulse</ComboBoxItem>
                </ComboBox>
                
                <TextBlock Text="Amplitude (A)" />
                <TextBox Padding="2"/>
                
                <TextBlock Text="Period (T)" />
                <TextBox Padding="2"/>

                <TextBlock Text="Duration (d)" />
                <TextBox Padding="2"/>

                <TextBlock Text="Start Time (t)" />
                <TextBox Padding="2"/>

                <!-- Buttons -->
                <Grid Margin="0 10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="Generate" Margin="0 0 10 0" />
                    <Button Grid.Column="1" Content="Save" />
                    <Button Grid.Column="2" Content="Load" Margin="10 0 0 0"/>
                </Grid>

            </StackPanel>
            
            <!-- Charts -->
            <StackPanel Grid.Column="1" Margin="10 0 0 0"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf" 
               >
                <Grid Height="500">
                    <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Right" >
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="f(x)" LabelFormatter="{Binding YFormatter}"></lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="t[s]" Labels="{Binding Labels}"></lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>
